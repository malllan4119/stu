server:
  port: 8080

spring:
    datasource:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3306/act
        username: root
        password: root

mybatis:
  typeAliasesPackage: cn.tedu.entity
  mapperLocations: classpath:mappers/*.xml

logging:
  level: 
    cn.tedu.mapper: debug
